<Page x:Class="Navigation_View.AddFriendPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Navigation_View"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      RequestedTheme="Light"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox x:Name="NameTextBox" PlaceholderText="Nhập tên" Width="200" Margin="5"/>
            <TextBox x:Name="EmailTextBox" PlaceholderText="Nhập Email" Width="200" Margin="5"/>
            <TextBox x:Name="PhoneNumberTextBox" PlaceholderText="Nhập số điện thoại" Width="200" Margin="5"/>
            <Button x:Name="AddButton" Content="Add" Width="200" Margin="5" Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
